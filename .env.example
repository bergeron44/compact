# ─── Embedding Backend ────────────────────────────────
# Hugging Face API key (required for the embedding server)
HUGGINGFACEHUB_API_KEY=hf_your_api_key_here

# Embedding model (defaults to sentence-transformers/all-MiniLM-L6-v2 → 384 dims)
# For Dell DevX embeddinggemma-300m use 768 dims
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Server port (defaults to 3000)
PORT=3000

# ─── Frontend (Vite) ─────────────────────────────────
# URL of the embedding API (used by the frontend; defaults to /api via Vite proxy)
# VITE_EMBED_API_URL=/api

# ─── Cache Service ────────────────────────────────────
# Cache Service URL (Python FastAPI service)
VITE_CACHE_SERVICE_URL=http://localhost:8001

# Feature flag: use new cache service
# Set to 'true' to use Python service, 'false' for IndexedDB
VITE_USE_CACHE_SERVICE=false

# ─── Embedding Provider (for cache service) ───────────
# CURRENT: Using HuggingFace (we have API key)
# See server/.env for HUGGINGFACEHUB_API_KEY

# FUTURE: Dell GenAI (when credentials available)
# Uncomment and configure when Dell credentials are obtained:
# DELL_USE_SSO=false
# DELL_CLIENT_ID=your_client_id_here
# DELL_CLIENT_SECRET=your_client_secret_here
# DELL_EMBEDDING_MODEL=granite-embedding-278m-multilingual

# ChromaDB Storage Directory
CHROMA_PERSIST_DIR=./chroma_data

