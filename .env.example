# ─── Embedding Backend ────────────────────────────────
# Hugging Face API key (required for the embedding server)
HUGGINGFACEHUB_API_KEY=hf_your_api_key_here

# Embedding model (defaults to sentence-transformers/all-MiniLM-L6-v2 → 384 dims)
# For Dell DevX embeddinggemma-300m use 768 dims
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Server port (defaults to 3000)
PORT=3000

# ─── Frontend (Vite) ─────────────────────────────────
# URL of the embedding API (used by the frontend; defaults to /api via Vite proxy)
# VITE_EMBED_API_URL=/api
