# ============================================================
# EMBEDDING PROVIDER CONFIGURATION
# ============================================================
# The service supports multiple embedding providers.
# Priority: HuggingFace > Dell GenAI > Placeholder (zeros)

# --- Option 1: HuggingFace (CURRENT - has API key) ---
# Using existing HuggingFace infrastructure for real embeddings
HUGGINGFACEHUB_API_KEY=hf_your_api_key_here
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# --- Option 2: Dell GenAI (FUTURE - when credentials available) ---
# For Teams plan (Client ID/Secret):
# DELL_USE_SSO=false
# DELL_CLIENT_ID=Insert_your_client_id_here
# DELL_CLIENT_SECRET=Insert_your_client_secret_here

# For Individual plan (SSO):
# DELL_USE_SSO=true

# Dell Embedding Model Selection
# Available: nomic-embed-text-v1, embeddinggemma-300m, granite-embedding-278m-multilingual
# DELL_EMBEDDING_MODEL=granite-embedding-278m-multilingual

# Enable server-side token refresh (alternative to client-side with httpx)
# ENABLE_TOKEN_REFRESH_AT_SERVER_SIDE=false

# ============================================================
# CHROMADB CONFIGURATION
# ============================================================
# ChromaDB Persistence Directory
CHROMA_PERSIST_DIR=./chroma_data

# ============================================================
# LLM PROVIDER CONFIGURATION
# ============================================================
# The service builds a resilient fallback chain:
#   Gemini (multi-key) → OpenRouter (100+ models) → Dell → Mock
# The first provider that responds wins. Mock always last.

# --- Option 1: Google Gemini (External) ---
# Supports multiple comma-separated keys for rate-limit rotation
# Get keys: https://aistudio.google.com/apikey
GEMINI_API_KEY=key1,key2
GEMINI_MODEL=gemini-2.5-flash

# --- Option 2: OpenRouter (100+ models, many free) ---
# Get key: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-your_key_here
# OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free

# --- Option 3: Dell GenAI (Internal) ---
# Uses same credentials as embedding provider (DELL_CLIENT_ID, etc.)
# DELL_LLM_MODEL=llama3.1-70b-instruct
